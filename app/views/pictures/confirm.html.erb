<h3>以下の内容で送信する</h3>
<%= form_with(model: @picture,local: true,url:) do |form| %>
    <%= form.hidden_field :image %>
    <%= form.hidden_field :conetnt %>
    
    <p>:<%= @picture.image %></p>
    <p>:<%= @picture.content %></p>
    <%= form.submit '登録する' %>
<% end %>
